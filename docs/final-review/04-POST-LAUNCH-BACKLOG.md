# Post-Launch Backlog

## POST-LAUNCH P1

- Remove Backup V1 UI completely after all pilots on V2.
- Unify conflict queue into SQLite-only UI.
- Unify attachment metadata to `attachments_meta`.
- Archive conflicting older `docs/integration-v2*` PASS matrices with a single canonical index.
- Reduce Owner Hub density (support tools vs daily ops).
- Single Feature Registry source (drop inline duplicate).
- Expand modal-shell adoption to all remaining overlays.
- Operator-friendly log export pack from Installed EXE.

## POST-LAUNCH P2

- Split mega `index.html` into maintainable renderer modules.
- Remove transitional `db-bridge` / LS repository adapters after soak.
- Clarify/merge Cloud V2 daily JSON backup into Backup V2 schedule UX.
- Automated screenshot responsive suite (Playwright against packaged app).
- Perf profiling dashboards for large outbox / large attachment sets.
- Reduce BootFlow copy complexity / progress chrome.

## FUTURE V3

- True multi-window / multi-profile support.
- Server-mediated sync (if Drive limits become product ceiling).
- Plugin marketplace / deeper finance ERP connectors.
- Full offline-first CRDT experiments (only if LWW/policies prove insufficient).
- Native auto-update channel with signed releases beyond current NSIS.

## NOT NEEDED

- Rebuilding repository / resetting git history.
- Second parallel activation wizard.
- Making Google Sheets operational SoT.
- Reintroducing optimistic localStorage cache for core tables.
- Primary/secondary device hierarchy (current peer registry is correct direction).
- Portable EXE as official channel (NSIS is the product installer).
